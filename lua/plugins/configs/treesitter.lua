local options = {
  ensure_installed = { "lua", "luadoc", "vim", "vimdoc", "json" },

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
